services:
  - type: web
    name: echoland
    runtime: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: npm run start --workspace server
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: AI_PROVIDER
        value: heuristic
